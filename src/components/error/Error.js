import React from "react";

export const Error = () => {
    return (
        <div className="error">
            <p className="message">Sorry, something went wrong.<br/> Reload the page or try later.</p>
        </div>
    );
}